# Environment and secrets (never commit real keys)
.env
.env.local
.env.*.local
api/.env

# Keep .env.example as template (no negation needed if we don't use .env.*)
# If you use .env.development etc, add those to ignore and keep only .env.example tracked.

# MCP and local config (can contain API keys / tokens)
.mcp.json
.claude/

# Dependencies
node_modules/

# Build outputs
api/dist/
program/target/

# Anchor / Solana
.anchor/
test-ledger/

# IDE and OS
.idea/
.vscode/
.DS_Store
*.log
